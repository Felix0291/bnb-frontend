import { useEffect, useState } from "react";
import { supabase } from "../lib/supabaseClient";

const UserList = () => {
    return (
        <>
        <h1></h1>
        </>
    );
};

export default UserList;